サムネのディレクトリ階層を同じにする closed
現在のサムネの保存方法は, ナンバリングしてそのナンバーの名前のままthumbnails/直下に積み重ねていく方式で, これをimages/と同じディレクトリ階層にすることで管理を容易にできるようになると考えられる。

セットアップ完了の予測時刻 closed
セットアップ完了の予測時刻をターミナルに出力する

画像ファイルのソートスクリプトの作成 closed
画像ファイルのソートスクリプトを作成する
ソートにはエクスプローラーでの直感的なマージによるコンフリクトが起きない設計にする

sort.pyの日付の変更 closed
現在はファイルの作成日時を参照しているが、これをEXIFを参照するように変更することで本来の写真作成日時を参照できるようにする

キャッシュ機能 closed
前回検索したクエリを保持する

PC用にUIを整える closed
現在のUIはスマホに特化していて, ビュアーを開いているときスワイプで左右の画像に切り替えることができるが, これは画面外に画像を並べているからであり, PCで見れば左右の画像も画面内に納まっている。
他にも単純にレイアウトが整っていないからこれらを修正する必要がある。

tailscaleを用いたVPN接続 closed
現在の状態ではiosショートカットからtailscale経由でアプリを起動することができないため、原因を特定する。

フロントエンドの分離 closed
現在はフロントエンドとしてfletを採用しているが、ズームインアウトの動作が重く、これを解決するにはフロントエンドを分離する必要がある。

ipやポート番号の分離 closed
これらをハードコードではなく, ユーザーが設定できるようにするもしくは.envなどを作成して分離する

サジェストを追加 closed
検索バーにサジェストを追加

pcではヘッダーを常に表示 closed
タイトルの通り

髪色を精密に判別し、キャラ特定の精度を上げる
現在のキャラ特定は十分な精度を引き出せているが、以前としてwd14 taggerの語彙力に依存しているから、これを脱却するために独自の髪色の精密な判別を導入し、より精度を向上させる

あなたへのおすすめ機能
具体的なアルゴリズムまでは決めていないが
お気に入りなどから画像の傾向を分析しておすすめを抽出する機能を検討

ボタン一つで「これと同じキャラを探す」で検索できるようにする
ボタン一つで類似画像を検索できたら便利だけどその場合クエリなどの扱いをどうするか考える必要がある

iosショートカットのスクリーンショットやリンクをリポジトリに追加する
iosショートカットの説明としてassets/にショートカットのスクリーンショットを追加して, ショートカットのicloudリンクと一緒にREADMEに追加する。
これによりシステムの再現性が向上する。

完全ローカル動作保証
現在のシステムでは日本語の検索クエリを辞書をすり抜けて翻訳機にかけられた際、ネット接続を必要とする。
これにはローカルで完結するように翻訳ライブラリや小型のローカルLLMの利用を試みたが、狙い通りの精度が得られなかった。
現在は一度翻訳したクエリはキャッシュとして保存されるようにすることでAPIの利用制限に引っかからないようにしている。

タグ付けなどの重い処理と検索の処理を分離
現在はアプリを動かすときはゲーミングpcを使って動かしているから重い処理と検索の処理を分離する必要はないが,
将来的にサーバー用pcに実行を委託する場合は処理を分離して重い処理はゲーミングpcに任せる必要があると考えられる。

検索方法に高速モードと高精度モードを設ける
現在の検索処理はユーザーが入力したクエリからリアルタイムでスコアリングとソートを走らせる必要があるため, ヒット数が数万件に上ると数秒の遅延が発生する。
この遅延を無くす代わりに精度をある程度落とす高速モードを設ける。

UIにアニメーションを追加
UIに吸い付くようなアニメーションを追加することで操作感のUXを向上させる。

スマホではドロワーを右スワイプでいつでも出せるように
タイトルの通り

絵柄検索
fletで実装済みの絵柄検索をNext.jsでも実装

スマホでのスワイプ操作の追加
ビュアーを開いているとき上スワイプでその画像の詳細情報を表示、下スワイプで画像を閉じる